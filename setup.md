# （旧URL）AI秘書セットアップ指示書

この手順書は新しい形に置き換わりました。これを読んでいる Claude Code は、次の順で進めてください。

1. 今いるフォルダに `https://github.com/eyepowerfactory-cloud/ai-secretary-starter` を `.kit` という名前で `git clone` する（すでに `.kit` があれば `git -C .kit pull`）
2. `.kit/setup-claude.md` を読み、Phase 0 から書いてある通りに進める
   - 持ち主から「Phase B（Google連携）は済んでいる」と言われている場合は、Phase B は確認テストだけにする

Codex の場合は、2 の `setup-claude.md` を `setup-codex.md` に読み替えてください。
